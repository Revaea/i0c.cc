# Analytics domain

Shared analytics event validation, canonical event types, and query result types used by collector hosts and storage plugins. The package contains no database driver, framework cache, or provider SDK.
