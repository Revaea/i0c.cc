# 外部 Runtime 插件夹具

此私有包模拟独立开发的 Runtime 平台插件和 Feature 插件。它提供标准 Manifest、Runtime、Feature 和 Installation 入口，用于证明 `apps/runtime` 可以从构建期安装配置生成这两类插件，而无需增加插件专属宿主源码。

它位于插件子系统的测试夹具目录中，不是受支持的可部署平台。此包沿用仓库的 Apache-2.0 许可证。
